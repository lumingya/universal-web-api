-r requirements.txt
pytest>=8,<10
# FastAPI/Starlette TestClient 与 ASGITransport 端到端测试依赖（tests/test_review_fixes_*.py 等）。
httpx>=0.27,<1
# Optional local Chromium UI/DOM integration tests (not required by the server).
playwright>=1.50,<2
