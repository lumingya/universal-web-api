# 环境配置（含敏感信息）
.env
*.local
*.local.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
ENV/
.venv/
temp/
.pytest_cache/

# 浏览器配置（含用户登录态，绝对不能提交！）
chrome_profile/

# 自动更新备份
backup_*/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Visual Studio
**/.vs/
bin/
obj/
*.user
*.suo
*.pdb
*.cache

# 日志
*.log
logs/

# 运行时统计（持久化请求计数等）
config/app_stats.json
config/model_name_overrides.local.json
config/arena_model_aliases.local.json
config/request_history.json
config/command_results.local.json
config/commands.json
config/sites.local.json
config/*.tmp
config/marketplace_cache.json

# 系统文件
.DS_Store
Thumbs.db

# 备份文件
*.bak
*.backup
*.bak_*

# Node.js
node_modules/

# 排除打包输出和打包脚本
dist/
build_release.py

# Git 提交脚本（不需要提交）
git提交.py

download_images/
image/
tmp/
scratch/

# Windows folder metadata
desktop.ini

# Agent 协作与网桥配置文件
agent_collaboration_rules.md
.agent_bridge.json

# 内网穿透脚本与批处理文件
开启公网访问(8199端口).bat
开启公网访问(免登录无警告版).bat
开启公网访问(Cpolar极速版).bat
start_ngrok.py
start_cloudflare.py
start_cpolar.py

# Playwright
.playwright-cli/

# 自定义与本地工作流扩展脚本
custom_scripts/
*.custom.js

# JS 脚本与用户脚本（UserScripts）
/js/
*.user.js

# 浏览器回归测试生成的截图
/test-artifacts/

# 本地脚本目录：仅跟踪随仓库发布的脚本（白名单）；其余（含 scripts/custom/、arena_models_cache.json 等运行时缓存）一律本地
/scripts/*
!/scripts/bundle_workflow_studio_css.py
!/scripts/upgrade_gemini_deepseek.py
!/scripts/build_sites_index.py

# Tests：tests/ 下默认全部跟踪（R0-5）。原先的白名单让 v3.0.0 的 12 个测试从未入库（路线图 N3）。
# 个人临时脚本或输出请使用下列命名，避免误提交：
/tests/local_*
/tests/*_local.py
/tests/_scratch/
/tests/output/

# R1-2：站点配置迁移备份、回收站与临时文件（config/sites/ 其余内容正常跟踪）
/config/*.migrated-*.bak
/config/sites/.trash/
/config/sites/*.tmp
