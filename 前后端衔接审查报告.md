# 前后端衔接审查与修复报告

审查对象：`普遍反代/新测试版`（FastAPI 后端 + Vue3 CDN 前端）
审查方式：6 个子代理按模块并行审查 → 逐条交叉验证（含用真实 `config/sites.json`、`request_history.json`、`browser_config.json` 回放）→ 修复 → 独立子代理对抗性复核修复本身 → 二轮修复
改动规模：**28 个文件，约 1571 行**（已按各文件原有行尾风格归一，避免 git diff 噪声）

---

## 一、已修复的 bug

### 会造成数据丢失 / 静默覆盖用户配置（最严重的一类）

| # | 问题 | 根因 | 影响 |
|---|---|---|---|
| 1 | **可视化工作流编辑器保存时删除不认识的步骤** | `workflow-editor-inject.js` 加载时 `return` 跳过 `READONLY_HINT`/`STREAM_OUTPUT`，保存时从 `state.steps` 整体重建，后端 `preset_data["workflow"] = new_workflow` 整体替换 | gemini.google.com 的 3 个预设 + deepseek 实验性预设共 4 条 `READONLY_HINT` 会在"挪一下球就保存"时永久消失。已改为保留原下标并在保存时回插，保存提示会说明"已保留 N 个不支持的步骤" |
| 2 | **保存"浏览器常量"用陈旧快照整体覆写 `tab_pool`** | `browserConstantsRaw` 只在首屏加载一次，而「标签页池」页签通过 `PUT /api/tab-pool/config` 改的是同一个 `browser_config.json` | 先在标签页池建路由组 → 回设置页改个数字保存 → 路由组、URL 排除、分配模式全部回退且立即生效。已改为保存前只刷新 merge 基底（不覆盖用户未保存的编辑） |
| 3 | **`GET /api/tab-pool/tabs` 异常时返回 200 + 伪造的配置默认值** | except 分支编造 `allocation_mode: "first_idle"`、`preserve_error_tabs: False`、`route_groups: []` | 浏览器掉线时页面无错误提示，用户随手一改就把真实配置翻转、把已有路由组抹成空。已改为只回 `error` + 静态选项表；前端加 `configLoaded` 守卫，错误态下 8 个写操作按钮全部禁用 |
| 4 | **JSON 预览保存路径不 flush 面板草稿** | `flushMutableSectionDrafts()` 全项目只有一处调用 | 在「文件粘贴」面板改了值不点保存、打开 JSON 预览 → 显示旧值、保存写回旧值、你那次编辑被彻底丢弃且无提示。已抽出 `flushConfigTabDrafts()` 在 3 处调用 |
| 5 | **请求历史被硬编码截断到 200 条** | `_append_sorted_history_record_unlocked` 的 `max_records=200` 默认值，两个调用点都没传参 | 你的配置是 2000，UI 也显示"上限 2000 / 保留容量 10%"，实际永远停在 200；若磁盘已有 2000 条，**一个新请求就会 `del records[:1801]` 销毁 1801 条** |

### 前后端字段/枚举不一致

| # | 问题 | 影响 |
|---|---|---|
| 6 | **`stream_config.send_confirmation` 已迁移到 `file_paste`，前端仍从旧位置读** | 实测 29 个预设里 `stream_config.send_confirmation` 出现 **0 次**、`file_paste.send_confirmation` 出现 **18 次**。「流式配置 → 发送确认」面板显示的全是硬编码默认值，改任意一项就把 19 个默认值整份写回、覆盖真实配置——arena.ai 三个 Battle 预设存的 `retry_on_unconfirmed_send: false`（面板自己的说明文字就警告 Battle 模式必须关）会被静默打开 |
| 7 | **`audio_network_capture.max_payload_bytes` 前端有输入框、后端校验器完全不读** | 改成 50MB → UI 立刻显示 50 → 刷新变回 10。旁证：29 个预设该值全是 `10485760`，零离散度 |
| 8 | **`audio_network_capture.extractor` 默认值取反、下拉只有 2 选 1** | 后端默认 `voicegenie_binary_stream`，前端写 `voicegenie_ogg_pages` 且下拉只有后者。豆包朗读等 3 个预设存的是 binary_stream，打开面板下拉渲染空白，点一下就把音频解析器永久降级 |
| 9 | **`max_size_mb` 连续区间被做成 4 值下拉** | 后端 1–100，前端只有 5/10/20/50。aistudio / grok 三个主预设存的是 100，会渲染空白并在任何交互时被砍到 ≤50。已换成 number input |
| 10 | **`debounce_seconds` 的 `\|\| 2`** | `min="0"` 宣称合法，但 `parseFloat("0") \|\| 2` 恒为 2，想关防抖关不掉 |
| 11 | **`run_policy` 枚举 image/video 各少一个值** | 后端 5 个值，前端 audio 给全、image/video 缺 `probe_if_trigger_found` |
| 12 | **前端元素定义常量只有 6 项，后端 11 项** | 缺 `retry_send_btn`/`stop_btn`/`upload_btn`/`file_input`/`drop_zone`。接口偶发失败后再点保存，只会 POST 6 条，用户对其余项的自定义被静默重置为后端默认 |
| 13 | **`STREAM_OUTPUT` 工作流步骤后端六处支持、两个前端都无选项** | 已有该值的步骤在面板里渲染成空白控件，碰一下下拉就被改成别的类型 |
| 14 | **标签页状态 `closed` 前端无分支** | 手动关掉受控标签页后卡片显示英文 `closed`，颜色与"未知"同色 |
| 15 | **`route_group` 路由方式开关对标签页卡片无效** | 5 个路由方式里只有它漏了显示守卫，取消勾选后帮助列表消失但卡片上的路由行还在 |
| 16 | **`exact_url_preset` 缺席路由说明列表** | 有开关、有端点，唯独没有说明 |
| 17 | **`browserStatus.tab_title` / `tab_url` 后端从未返回** | 首页浏览器卡片恒为兜底文案。已改用 health 里真实存在的 `tab_pool` 概览（总数/忙碌/空闲） |
| 18 | **「正在执行」KPI 恒为 0** | 三重错：`'pending'`/`'processing'` 不在后端任何枚举里；真实存在的 `queued` 没检查；最根本的是请求历史结构上只含终态记录（实测 200 条全是 completed/failed/cancelled）。已在 `/api/system/stats` 增加 `running_count`/`queued_count` 并改读实时字段 |
| 19 | **「包含多模态」过滤器不影响状态页签计数** | 实测 200 条里 109 条是多模态。取消勾选后页签显示"全部 200"、分页行显示"共 91 条" |
| 20 | **请求详情合并白名单漏 `error_message`/`summary`** | 详情接口拿回了完整 10000 字错误，但合并不含这两个键，抽屉里永远显示列表版被砍到 800 字的版本——工具调用失败最关键的尾部信息被切掉 |
| 21 | **详情抽屉的工具调用错误面板是死代码** | `__toolCallingErrorInfo` 只挂在视图记录上，而详情用的是原始记录，`v-if` 恒 false。卡片列表能看到结构化解释，点进详情反而退化成裸报错 |
| 22 | **日志 INFO 档漏掉全部 `[AI]` 日志** | `normalizeLogLevel` 把带 `[AI]` 的 INFO 重分类成 `'AI'`，过滤却按重分类后的值走。已改用 `rawLevel` 兜底 |

### 后端健壮性 / 安全

| # | 问题 | 影响 |
|---|---|---|
| 23 | **数字输入框清空 → `APP_PORT=` → 重启后 `int("")` 崩溃** | 保存即 `os._exit(3)` 重启，然后在 `uvicorn.run(port=...)` 处崩，控制面板彻底打不开，只能手改 `.env`。已加 `_env_int`/`_env_float` 安全解析，替换全部 7 处裸 `int(os.getenv(...))` |
| 24 | **`AUTH_TOKEN=00123456` 被读成 int、回写成 `123456`** | 令牌被静默改写，所有客户端认证失败。已改为只对白名单里的数值键做类型推断 |
| 25 | **打开认证开关但 token 留空 → 全部接口 500 + 自动重启 → 面板自锁死** | 已在写盘前校验并返回 400 |
| 26 | **`POST /api/settings/env` 可通过值里的 `\n` 往 .env 注入额外行** | 例如注入 `DASHBOARD_AUTH_ENABLED=false` 关掉认证。已拒绝换行 + 校验新增/变更键的键名格式 |
| 27 | **`DASHBOARD_AUTH_*` 的合成值被固化写盘** | 破坏「留空即沿用 AUTH_*」语义：保存一次后改 `AUTH_TOKEN`，面板令牌不再跟随。已改为后端用独立的 `effective` 字段返回、前端只在 .env 确实缺该键时采纳 |
| 28 | **`SELECT_MODEL` 编辑器测试通过、生产运行硬失败** | 执行器明确支持空 selector（有硬编码兜底），但生产 runner 的 missing_selector 豁免名单漏了它 |
| 29 | **保存工作流不校验 action 名称** | `STREAM_WAIT` 拼成 `STREAM_WIAT` 会保存成功、面板空白、运行时被 debug 日志静默吞掉，表现为"跑完了但什么都没发生"。已加 400 校验（存量 186 个步骤全部合法，无兼容问题） |
| 30 | **编辑器测试的 context 缺 `model`** | `SELECT_MODEL` 必然 early return 却仍计入 `executed_steps`，测试显示"已测试 N 步成功"而模型切换一行没跑。已两侧透传 + 单独计跳过数 |
| 31 | **删除预设不清理在用标签页的引用** | 标签页的预设下拉渲染成空白且用户无法脱困。后端补了清理、前端加了兜底 |
| 32 | **版本检查失败时 `latest_*` 残留** | 界面同时显示"最新版本号"和"检查失败"，语义矛盾 |
| 33 | **`switch_proxy` 等 4 处 `command_id` 未 `encodeURIComponent`** | 同文件另外 4 处是对的。手写 id（配置里确有 `cmd_arena_ip_rotate_q4` 这类）含特殊字符时会 404 |
| 34 | **切换动作类型不清理旧字段，`selector` 跨类型串味** | `selector` 在 `click_element` 里是 CSS 选择器、在 `switch_proxy` 里是 Clash 代理组名。改类型后 `#submit-btn` 会被当成代理组名。已按语义分组定向清理（同语义切换如 `click_element → write_element` 不会误清选择器） |
| 35 | 后端 `send_btn` 描述有错字「发送图标 **of** 按钮」 | 已改为「发送图标的按钮」 |

---

## 二、补齐的前端入口（后端已支持、原来只能手改文件）

### 系统设置页
- **`CMD_ALLOW_UNSAFE_PYTHON_COMMANDS`**（新增「高危开关」分组）——为真时跳过 Python 沙箱、以完整 `__builtins__` 执行任意脚本。`.env.example` 原来默认写的是 `true`，照抄就是开启状态，而面板既不显示也无法关闭。已补入口 + 醒目警告，`.env.example` 默认改 `false`
- **`TAB_RECOVERY_*` 8 项**（新增「标签页自动恢复」分组）——含 API 地址/密钥/模型/重试次数等。开启后会把出错标签页的**截图发送到外部 API**，已在 desc 里写明隐私提示
- **`PAGE_INTERACTION_*` 3 项**（新增「页面交互节流」分组）——`PAGE_INTERACTION_MAX_CONCURRENT` 默认 3 是**跨标签页的全局并发上限**，与你能调的「最大标签页数」（12）直接冲突：池子开 12 个但同时只有 3 路在做页面交互，其余排队 20 秒后走降级。原来只能手改 JSON
- **`REQUEST_ZOMBIE_TTL`**——卡死请求多久判定为僵尸（默认 600 秒）。这段时间里监控页看不到任何异常记录
- **「立即检查」更新按钮**——`POST /api/update/check` 会真正联网重查，而前端原来只调 GET（后端注释明确写着"不会访问 GitHub"），没有任何主动检查入口

### 指令编辑器（6 个动作类型的参数原来完全没有 UI）
- **`run_js`**（配置里用了 15 次）补 5 个参数：`retry_on_results`/`retry_attempts`/`retry_wait_seconds`/`retry_after_refresh`/`fail_on_falsy`。其中 `fail_on_falsy` **默认为 true** ——你在 UI 里建的 `run_js` 一旦 JS 返回 `undefined` 就被判失败，而 UI 上完全看不到这个开关，排障极易迷失
- **`click_captcha_challenge`** 原本零控件（还被误分到「其他」分类），补了 `max_attempts`/`random_radius`/`timeout_sec` 面板 + 修正分类
- **`abort_task`** 原本只有一行写死的说明文字，补 `reason`/`stop_actions`（后端支持 `stop_actions: false`，说明文字原来是错的）
- **`execute_workflow`** 补 `timeout_sec`/`workflow_priority`（原来超时只能靠环境变量全局设定，慢站点必然超时且无处可调）
- **`run_js_file`** 补 `teardown_js`（预注入脚本的卸载钩子，配置里**已在使用**却完全不可见）+ `encoding`/`fail_on_falsy`
- **`http_request`** 补 `consume_response`（决定 `save_as` 能否拿到正文）

### 其它
- 元素定义补齐到 11 项；`STREAM_OUTPUT` 工作流步骤补入下拉；`probe_if_trigger_found` 补入 image/video；`voicegenie_binary_stream` 补入音频解析器下拉；可视化编辑器的 MODEL 球补了元素拾取入口和「测试模型名」输入框

---

## 三、`.env.example` 清理

- 删掉了**前 25 行整段重复**（`CURRENT_VERSION`/`APP_PORT` 等 6 个键各出现两次）。三条加载路径对重复键的取值顺序还不一致——`config.py` 是首次生效，`start.py` 和 `system.py` 是最后生效。一旦只改其中一份，启动器/服务进程/控制面板会读到三套不同的值
- `AUTH_ENABLED=your-secret-here` / `DASHBOARD_AUTH_ENABLED=your-secret-here` → `false`（这是布尔量，占位符会让人以为已配好，实际判定为 false）
- 注释掉 6 个后端零引用的死变量：4 个 `NGROK_*`、`CURRENT_VERSION`、`UPDATE_PRESERVE`。**其中 Ngrok 那块最危险**——注释写着「Basic Auth 账号/密码（留空则不启用密码保护）」，配了会让人以为穿透入口有密码保护，实际没有任何代码读取

---

## 四、发现但**没有**修的（附判断理由）

1. **提取器管理 UI 整套未接入**（`ExtractorTab.js` / `ExtractorPanel.js` 既不在 `index.html` 也未注册，6 个后端端点零调用方，`ExtractorPanel` 还指引用户"前往「提取器」Tab"——那个 Tab 不存在）。
   没修的理由：这是恢复一整个功能页而非 bug 修复，且实测 **29 个预设的 `extractor_id` 全是 `None`**，该功能处于休眠状态。要不要恢复取决于你还打算不打算用它。

2. **`CommandsTabTemplate.js` 少一个 `</div>`**（根 div 到 EOF 才被自动闭合）。
   没修的理由：缺失点在**我没碰过**的「触发机制」区块，属既有问题；实测用生产版 Vue 编译**不抛异常**（parser 在 EOF 自动闭合），实际影响只是「动作与执行」等区块多吃一层 margin。在 188KB 模板里盲改嵌套有改坏布局的风险，建议你对着页面确认后再补。

3. **`preservedSteps` 按原始绝对下标回插**——用户在可视化编辑器里删掉前面的球后，被保留的 `READONLY_HINT` 相对位置可能微移。内容不会丢，且这两类步骤在运行期都不依赖前后紧邻关系。

4. **后端布尔解析有三套不同规则**：`config.py` 是白名单（空串回落默认）、`tool_calling_common.py` 是**黑名单**（拼错的值一律判 True）、`command_engine_actions.py` 是白名单但空串返回 False。建议抽一个共用 `env_bool()`，但这是跨模块重构，改动面比本次审查的范围大。

5. **`extractors.json` 的 `enabled: false` 对三个内置提取器无效**（`__init__.py` 无条件注册），且 `set_default` 允许把禁用项设为默认、`import_config` 却拒绝同样的配置。与第 1 条同因——功能休眠，暂不处理。

---

## 五、验证情况

- 8 个 Python 文件 `py_compile` 通过（在你机器上用本地 python 复测也通过）
- 25 个 JS 文件 `node --check` 通过；24 个组件模板用 `@vue/compiler-dom` 编译（除上述既有的 div 问题外零错误）
- 全量扫描：无 `v-if`+`v-for` 同元素、无孤立 `v-else-if`、无未闭合 `<template>`、无重复 `:key`
- 用 `prefixIdentifiers` 提取所有模板标识符，逐一比对 props/data/computed/methods —— 无缺失引用；新增的 prop/emit 在 `index.html` 双向绑定完整
- 新增的 12 个 ENV 键和 4 个浏览器常量，默认值逐项核对 `app/core/config.py` / `app/api/system.py` 的真实默认值，全部一致
- 用真实 `config/sites.json` 回放验证：action 名校验不会拒绝存量数据（186 个步骤全合法）、`max_payload_bytes` 的 64KB 下限不会截断现有值（29 处全是 10MB）
- 认证自锁校验跑了 7 组用例：「关闭全部认证」「只关服务认证」「改无关项」全部放行，只有「开着认证却清空令牌」才 400
- **一轮对抗性复核**由独立子代理执行，找出了我第一批修复引入的 2 个高危回归（发送确认编辑会视觉回滚、控制面板认证会被静默写成 false）和一个会导致**死循环**的 `continue`（`while` 循环里跳过了 `step_index += 1`）——均已在第二轮修好并重新验证

---

## 六、需要你手动做的两件事

1. **清理临时文件**（我这边的会话在最后一步断开了，没来得及移走）：项目根目录下的 `_review_snapshot.tgz`、`rev.part.aa`、`_review_fixes.tar.gz` 都是这次审查的中间产物，可以直接删掉。

2. **原文件备份在 `_backup_before_review/`**（28 个文件，2.0MB，按原目录结构存放）。确认新版本跑起来没问题后删掉即可；如需回滚，把里面的文件覆盖回去就行。
