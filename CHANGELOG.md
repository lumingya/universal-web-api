# 更新日志

## v2.9.5

> 本次是一个**大版本更新**：控制面板做了整体 UI 改版，新增首页与新手引导，
> 同时在标签页池、流式输出、工具调用、网络监听等核心链路上修复了大量问题。
>
> 升级建议：改版涉及大量前端资源，升级后请**强制刷新浏览器缓存**（Ctrl + F5）。

---

## ✨ 一、全新 UI（本次最大的变化）

本次控制面板的视觉与交互进行了完整改版，设计语言参考了开源项目
**[oh-my-ppt](https://github.com/arcsin1/oh-my-ppt)**（作者 arcsin1，一款纯本地的 AI 幻灯片生成与编辑工具）。
该项目以 **Apache License 2.0** 协议开源，本项目在其设计思路的启发下重新构建了界面样式，
特此致谢并注明出处。相关许可信息见其仓库的 LICENSE 文件。

### 1. 新的设计语言

- **配色整体换代**：由原来的冷蓝灰系改为**暖纸底 + 橄榄绿**主题（页面底 `#f7f3ea`、
  强调色 `#5d6b4d`），辅以赭石 / 陶土红 / 蓝灰三色承担警告、失败与提示语义，
  整体观感更柔和、长时间盯着不刺眼。
- **统一的排版体系**：字体统一为 Inter / 微软雅黑，圆角明显收敛（按钮 7px、卡片 8px），
  阴影改为低透明度暖调，字号与间距重新分级。
- **完整的深色模式**：新增独立的深色样式层，夜间不再是冷灰而是深墨绿；
  连同弹窗、标签页池、设置页等尚未重写的旧组件也一并统一到新色板。
  另外新增了**防闪白处理**，刷新页面时不会再先闪一下白色。
- **样式分层**：新增 `dashboard-migration.css` / `dashboard-reference.css` /
  `dashboard-dark.css` / `onboarding-guide.css` 四层样式，兼容层保证旧面板自动换肤。

### 2. 侧边栏重做

- 改为标准竖排主导航，共 7 个入口：**首页 / 站点配置 / 标签页池 / 请求监控 /
  命令系统 / 运行日志 / 设置**（命令系统首次进入侧边栏，站点配置带站点数角标，
  有新版本时设置项显示红点）。
- 支持**折叠成图标条并记住折叠状态**，下次打开保持。
- 底部聚合区：新手指南入口、日间/夜间模式开关、浏览器连接状态与刷新、
  面板认证状态、随当前页面变化的主操作按钮（新增站点 / 新建命令）、版本号。

### 3. 移动端与响应式适配

- 窄屏下侧边栏变为**抽屉式导航**，配半透明毛玻璃遮罩；
  新增移动端顶栏（汉堡菜单 + 标题 + 服务状态点，点击可刷新状态）。
- 请求监控表格在窄屏下**自动变为卡片流**，不再横向撑爆。
- 三档断点（1180px / 900px / 520px）逐级重排 Hero 区、指标条、图表与功能卡片。
- 新增 `prefers-reduced-motion` 支持与统一的键盘焦点描边。

### 4. 站点配置页重构

- 站点列表**从侧边栏搬到了站点配置页**，配独立工具栏：
  搜索框（输入即出候选、回车直达第一个）、站点计数、刷新、配置对比、导入/导出/新增。
- 当前站点改用**站点切换器下拉**切换，每项带首字母色块、友好名称与域名，
  悬停即可导出或删除。
- 原来一长串默认全折叠的面板，改为 **6 个分类页签**：元素选择器 / 媒体提取 /
  响应解析 / 输入处理 / 高级功能 / 请求工作流，不用再一路往下滚+逐个展开。
- 预设工具栏精简为「预设下拉 + 设为默认 + 新建预设 + 更多菜单」，长段说明默认收起。
- 文件粘贴 / 附件面板拆成三段可折叠分区，高级规则默认收起。
- 常见站点显示**友好名称**（如 `chat.deepseek.com → DeepSeek`、
  `www.doubao.com → 豆包 Doubao`、`chat.qwen.ai → 通义千问 Qwen` 等 14 个站点）。

### 5. 图标与文案

- 图标库扩充（新增 home / activity / globe / timer / server / 搜索 / 手册 / 月亮 / 太阳等），
  各面板标题里的 emoji 前缀全部替换为统一线性图标。
- 教程页同步降噪：去掉夸张措辞与 emoji 标题，改为简短陈述。

---

## 🏠 二、新增功能

### 1. 全新首页

打开控制面板不再直接落到站点配置，而是进入新首页：

- 顶部服务状态横幅（本地 API 已就绪 / 正在等待浏览器）；
- 四项健康指标：服务状态、可用站点数、累计请求数、平均响应耗时；
- 三张操作卡片：**一键复制 Base URL**、查看受控浏览器与标签页占用、查看最近 3 条请求；
- 底部最近活动条。

### 2. 首次使用新手引导（11 步）

第一次打开面板会自动弹出欢迎弹窗（约 3 分钟），分两个阶段：

- **阶段一「认识控制台」（6 步）**：用聚光灯依次高亮并讲解侧边栏各个模块；
- **阶段二「反代实操 SOP」（5 步）**：核心原理示意（网页 → 受控 Chrome → OpenAI API）
  → 在受控浏览器打开并登录目标网站 → 关键检查（展开网页侧边栏、页面语言设为中文）
  → 在标签页池选择路由方式（全局 / 域名路径 / Query 参数）→ 填 Base URL 与 API Key 接入客户端。

支持方向键 / 回车翻页、Esc 退出，带进度显示；之后可随时从**侧边栏底部「新手指南」**重新打开。

### 3. 请求监控页大幅增强

- **新增 Token 用量分析**：总 / 输入 / 输出 Token 与模型调用次数四张 KPI 卡、
  当天每小时输入输出折线图、每日 Token 趋势柱线复合图（近 7 天 / 30 天 / 全部）、
  模型用量占比环形图与 Top 8 图例。
- **新增趋势图与排行榜**：请求趋势折线（1 小时 / 24 小时 / 7 天）、分时段健康度条、
  站点 / 模型双维度 Top 10 排行榜、可折叠的系统负载面板（CPU / 内存 / 磁盘）。
- **新增搜索、筛选与分页**：关键词搜索（模型、域名、请求 ID、端点、摘要）、
  状态页签（全部 / 成功 / 失败 / 取消，带实时计数）、"包含多模态"开关；
  由"加载更多"改为真正的分页导航，并显示保留上限与容量占用。

### 4. 错误标签页隔离与 AI 自动恢复

标签页因 BUSY 超时被标记 ERROR 移出池后，底层标签页会先进入**隔离集合**，
在确认旧请求的工作线程真正退出前不会被重新拉回池中——彻底解决新旧请求
同时驱动同一个页面导致的串扰。**隔离机制始终生效，与开关无关。**

在此之上可选开启 AI 判断：把出错页面的截图交给支持图片输入的模型，
判断是否可以刷新恢复。新增 8 个配置项（控制面板「设置」页已提供完整入口）：

| 配置项 | 默认值 | 说明 |
| --- | --- | --- |
| `TAB_RECOVERY_ENABLED` | `false` | 关闭时旧线程退出即解除隔离，不调用任何 AI |
| `TAB_RECOVERY_API_URL` | 空 | 留空 = 本地模式，调用本服务自身，鉴权自动复用 `AUTH_TOKEN` |
| `TAB_RECOVERY_API_KEY` | 空 | 仅使用外部 API 时需要 |
| `TAB_RECOVERY_MODEL` | `gpt-4o` | 需支持图片输入 |
| `TAB_RECOVERY_MAX_ATTEMPTS` | `1` | 恢复成功后再次出错即永久隔离 |
| `TAB_RECOVERY_TIMEOUT_SEC` | `120` | 本地模式需完整浏览器往返，不建议过短 |
| `TAB_RECOVERY_WORKER_EXIT_WAIT_SEC` | `600` | 等待旧线程退出的上限 |
| `TAB_RECOVERY_REFRESH_ON_UNKNOWN` | `true` | 无法判断时是否仍尝试刷新恢复 |

> 隐私提示：若填写了外部 API 地址，出错标签页的**截图会发送到该外部服务**；留空则只调用本服务、不出网。

### 5. 日志"说人话"模式（默认开启）

- 每行日志新增 3 字符**级别徽标**（`INF` / `WRN` / `ERR` / `DBG` / `STM` / `NET` / `SUC`），
  一眼分辨级别；控制台配色重做，ERROR 整行红、WARNING 整行黄、DEBUG 暗灰降噪。
- 新增约 200 条日志翻译规则，覆盖请求生命周期、发送确认、标签页池、网络监听、
  工作流步骤、图片附件、工具调用、命令引擎等高频日志；**ERROR / CRITICAL 也会被翻译**，
  并按错误类别给出排查建议，技术原文保留在括号中。
- 该模式默认已开启。翻译只影响控制台与 Web 面板显示，**文件日志 `app.log` 始终保留技术原文**。
- 运行日志页新增 DEBUG 级别支持。

### 6. 设置页新增配置入口

以下能力后端早已支持，此前前端没有入口，本次全部补齐：

- **标签页自动恢复**（上文 8 项，带隐私提示）；
- **页面交互节流**：跨标签页的全局点击/输入/滚动并发闸门（开关、最大并发数、排队最长等待）；
- **僵尸请求判定时长**（30–3600 秒，默认 600），且**修改后无需重启**；
- **☠️ 高危开关**分组：目前含「允许非沙箱 Python 命令」，默认关闭并附醒目警告；
- **「立即检查」更新按钮**：此前只能读启动时的缓存状态，现在可主动联网重查。

### 7. 命令系统与工作流编辑器补齐

- `run_js` 新增「JS 重试与结果判定」面板（命中即重试的返回值、重试次数、
  重试前等待与刷新、返回假值判失败）；
- `run_js_file` 新增文件编码、假值判定与**卸载脚本 teardown_js**；
- `click_captcha_challenge` 首次提供参数编辑器（探测超时、点击随机偏移半径、最多尝试次数）；
- `abort_task` 新增原因标记与"执行后中断后续动作"开关；
- `execute_workflow` 新增超时秒数与优先级；`http_request` 新增 `consume_response`。
- 工作流可视化编辑器：工具栏新增**测试模型名输入框**（`SELECT_MODEL` 步骤终于能被真正测试到）、
  模型球新增**拾取元素**入口、**保留编辑器不支持的步骤**（如 `READONLY_HINT` / `STREAM_OUTPUT`，
  以前一保存就被静默删掉）。
- 工作流保存时**校验动作名**：动作名拼错或写成小写会直接返回错误并列出全部合法动作名，
  不再出现"工作流跑完但什么都没发生"。
- 前端内置兜底选择器定义补齐到 11 项（新增重试按钮、停止输出按钮、上传按钮、
  原生文件框、拖拽上传区）。

---

## ⚡ 三、性能与稳定性

- **服务不再被浏览器卡顿拖死**：`/v1/chat/completions` 的标签页扫描、`/v1/models`、
  请求内容记录、ffmpeg 媒体转码全部移出事件循环；标签页读接口的 CDP 全量扫描改为 1 秒节流。
  此前浏览器一卡或有人转码音视频，所有并发 SSE 流会一起冻住。
- **标签页获取使用独立线程池**（32 线程），不再因为占满 asyncio 默认线程池而拖垮整个事件循环。
- **僵尸请求定时清扫**：新增每 60 秒运行的后台清扫线程，低流量时段不再一直挂着超时请求。
- **请求监控历史容量放大 10 倍**：`REQUEST_MONITOR_MAX_RECORDS` 默认 200 → 2000，
  并修掉两处会强行回落到 200 条的截断 bug 与 API 层多余的二次上限。
- **Kimi 页面抓流改为增量传输**：由每 0.3 秒全量回传整段文本改为按偏移只传增量，
  长回复的传输量由 O(n²) 降为 O(n)。
- 结果事件写盘移出去重锁；Responses API 会话历史改为序列化存储，
  不再对含 base64 图片的完整历史做深拷贝。

---

## 🐛 四、问题修复

### 安全相关

- **CORS 允许任意来源时不再放行凭据**：`allow_origins=*` 配合 `allow_credentials=True`
  会导致任何网页都能跨源读取本服务，现在只有明确配置了具体来源时才开启 credentials。
- **控制面板设置保存新增三道校验**：① 配置值不能含换行（此前可借此向 .env 注入
  额外行，例如关掉认证）；② 键名必须合法；③ **认证自锁保护**——开启认证却留空令牌
  会被直接拒绝，此前这样保存后面板就再也打不开，只能手改 .env 恢复。
- **`.env` 数字推断收窄到白名单键**：此前 `AUTH_TOKEN=00123456` 会被静默改写成 `123456`。
- **`DASHBOARD_AUTH_ENABLED` 不再被静默写成 `false`**（此前保存任意一项都可能悄悄关掉面板认证）；
  `APP_DEBUG` 前端默认值与后端对齐，不再意外暴露 `/docs` 与详细错误栈。

### 流式输出

- 修复网络监听回退 DOM 时可能**把已输出内容整段重发**，客户端收到大段重复文本。
- 收尾观察窗口由固定 5 秒硬顶改为内容仍在生成时可延长至 15 秒，**长回复不再被提前截断**。
- 开启 `stream_options.include_usage` 时 usage **不再永远是 0**，Anthropic 流的
  `message_delta` 也补上了 `input_tokens`。
- 中途被取消的流不再补发 usage 与 `[DONE]` 伪装成正常完成，而是明确告知"响应不完整"。
- SSE 帧缓冲超限时改为整帧丢弃并告警，此前会拼出坏 JSON 转发给客户端。

### 请求生命周期

- **修复请求卡满 300 秒才超时**：工作线程结束标记在队列打满时可能丢失，
  导致流式消费空转到绝对超时并把已完成的响应误报为错误（4 条路径全部修复）。
- 内部取消现在返回结构化 499，而不是无结构的 500 或直接断连。

### 网络监听（5 项）

- **静默计时被心跳清零**：任意命中监听模式的响应（含心跳/埋点）都会重置静默计时，
  导致结束条件永远达不到、请求挂满硬超时；
- **429 / 403 子串误判**：改为解析真实 HTTP 状态码，URL 或 token 数里出现这些数字不再误触发；
- **保留的监听泄漏**：新增 120 秒存活上限；
- **响应尾部丢失**：完成判定成立后会强制补读一次；
- **取消被误报为超时**，不再触发不必要的 DOM 回退。

### 工具调用（6 项）

- **并行调用被静默丢弃**：部分通过 + 部分被拒时会直接返回子集，现在改为重试额度耗尽后才启用；
- **客户端 schema 非法时白白烧重试**：新增 `invalid_tool_schema` 错误码，识别后立即停止重试；
- **相同参数并行调用同一工具不再报错**，改为静默去重；
- **误判畸形工具负载**：让模型输出一段 OpenAI 响应示例 JSON 就会误触发重试，现已收窄判定；
- **XML 格式解析**：修复一处可导致灾难性回溯的正则（3KB 输入即可阻塞事件循环 10 秒以上），
  代码块里举例写的调用不再被当成真实调用；
- **重试提示里的假参数**：改为先解码再截断，不再把截断残片"修"出凭空捏造的参数。

### Anthropic 兼容层（5 项）

- `thinking` / `redacted_thinking` 块直接丢弃，不再把含 base64 签名的 JSON 塞进提示词；
- 内部错误保留原状态码并包装成标准 Anthropic 错误结构（3 个端点全覆盖）；
- 同一 delta 内多个文本片段直接拼接，不再插入原文不存在的换行；
- `count_tokens` 估算前剥离 base64 图片数据，带图请求的 token 数不再严重虚高。

### 配置保存

- **文件粘贴 / 开头注入面板的编辑可能被静默丢弃**（草稿未回写），已统一修复；
- **流式配置 → 发送确认**改动保存后被旧值盖回，已双向对齐；
- **切换站点瞬间保存会污染整站配置**（空预设名被当成站点级配置写入），已加守卫；
- **在「标签页池」页改的配置会被「设置」页保存时整块回退**（陈旧快照做合并基底），
  现在保存前会重新拉取最新基底；
- **标签页池配置加载失败时，前端兜底值会被当成真实配置写回磁盘**（尤其路由组会被空数组抹掉），
  现在未加载成功时一律禁用写操作并明确提示。

### 其他

- 非 ASCII（如中文）认证令牌导致 500，现在正常返回 401；
- 纯文本消息里出现 "image_url" 字样就被误判为图片声明而返回 400，已收窄为 `data:image`；
- 删除预设时同步清理标签页引用，不再指向已不存在的预设；
- Arena Direct 模型目录现在也能在 `/c/` 会话页识别，并**优先挑空闲标签页**；
- 模型路由：暴露名恰好等于通用 id（如 `gpt-4o`）的标签页现在改为精确匹配优先；
- 网络抓包配置的 `max_payload_bytes` 此前漏处理，设置后会被静默重置回 10MB；
- 请求监控：修复"正在执行"恒为 0、错误详情永远是截断版、工具调用错误面板恒不显示；
- 运行日志 INFO 档漏掉全部 `[AI]` 日志；
- 版本检查失败时不再同时显示"最新版本号"和"检查失败"；
- 「对比 main 分支」失败时区分"未装 Git / 压缩包解压安装 / 找不到 main 分支"三种情况给出中文说明；
- 工作流注入失败不再一律提示"网络错误"；
- 命令 ID 含特殊字符时接口调用出错；切换命令动作类型不再"串味"继承旧字段；
- 多处下拉/输入框取值域与后端不符（图片大小上限、防抖秒数、文件粘贴阈值、
  多模态运行策略、语音提取器、`STREAM_OUTPUT` 步骤等）已全部对齐。

---

## 🔧 五、配置与打包变更

- **`config/commands.json` 与 `config/marketplace_cache.json` 不再随仓库分发**
  （已加入 `.gitignore`，改为纯本地文件）。升级时不会再被作者的命令配置覆盖，也不会再产生 git 冲突。
- **`.env.example` 大整理**：删除整段重复粘贴的配置块；明确标注三处**后端根本不读**的配置
  （`CURRENT_VERSION` 实际读 `VERSION` 文件、`UPDATE_PRESERVE` 实际存于
  `config/update_settings.json`、全部 `NGROK_*` 当前版本尚未实现，**包括 Basic Auth 密码保护**）；
  修正 `AUTH_ENABLED` / `DASHBOARD_AUTH_ENABLED` 被误填成占位密钥的问题；
  `CMD_ALLOW_UNSAFE_PYTHON_COMMANDS` 默认值改为 `false` 并加安全警告。
- 浏览器常量补齐 4 个前端入口：`REQUEST_ZOMBIE_TTL`、`PAGE_INTERACTION_THROTTLE_ENABLED`、
  `PAGE_INTERACTION_MAX_CONCURRENT`、`PAGE_INTERACTION_MAX_WAIT`。

---

## 📖 六、文档更新

内置教程页（`/tutorial`）同步更新：

- **工作流动作表扩充 6 种节点说明**：`SELECT_MODEL`（按模型名分流）、`PAGE_FETCH`（页面直发）、
  `COORD_CLICK`（坐标点击）、`COORD_SCROLL`（模拟滚动）、`JS_EXEC`、`READONLY_HINT`（说明卡片）；
  `KEY_PRESS` 补充组合键下拉说明。
- **更正"必需 / 可选"语义**（原文档说反了）：卡片底部的「必需步骤」勾选框，
  **默认勾选 = 必需**，取消勾选才变为可选。
- **命令动作表更新**：新增 `click_element` / `click_coordinates` / `execute_command_group`，
  移除若干已废弃别名。
- **参数与默认值修正**：超时错误码更名（`acquire_tab_timeout → acquire_timeout`、
  `wait_first_response_timeout → first_response_timeout`）；DOM 模式全局硬超时更正为 600 秒；
  内容收缩阈值由"比例 0.3"更正为"3 个字符"；操作延迟默认值下调；
  辅助 AI 示例地址改为回指反代自身。
- 全站文案降噪，修正若干笔误。

---

## 致谢

- 控制面板 UI 设计参考自 [arcsin1/oh-my-ppt](https://github.com/arcsin1/oh-my-ppt)（Apache License 2.0），
  在此致谢原作者。本项目仅参考其设计语言与交互思路，相关代码按 Apache-2.0 要求保留出处说明。
