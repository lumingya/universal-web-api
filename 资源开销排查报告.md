# 资源开销排查报告

排查范围：`app/`（约 3MB Python 源码）、`js/`（页面内常驻用户脚本）、`static/js`（面板）、`config/`。
关注点按你的反馈优先排序：**浏览器（Chrome）侧开销** > **CPU 空转** > 内存。

---

## 一、已经改掉的（低风险，已写回本地文件）

改动集中在 3 个文件，`git diff` 可看全部内容。所有改动都通过了 Python 语法编译 + Node 语法检查，页面注入 JS 单独提取出来做了语法校验，并由一个独立的对抗性审查 agent 逐条复核过功能风险。

### 1. 页面文本采集的 O(n²) 遍历 —— 浏览器 CPU 的头号来源

`app/services/command_engine.py`，`_PAGE_CHECK_OBSERVER_JS` 与 `_PAGE_CHECK_SNAPSHOT_JS`。

```js
function collectText(node, parts, seen) {
    if (!node || seen.indexOf(node) !== -1) return;   // 数组线性查找
    seen.push(node);
```

`seen` 是普通数组，`indexOf` 是 O(n)，整页遍历退化成 **O(n²)**。这段代码由 MutationObserver 驱动，页面每有 DOM 变化就在 200ms 后跑一遍——**AI 流式输出时几乎是连续触发**。

实测（用等价实现在 Node 里跑，输出逐字节一致）：

| 页面节点数 | 旧（数组） | 新（Set） | 提速 |
|---|---|---|---|
| 5,000 | 3.9 ms | 1.25 ms | 3× |
| 15,000 | 34.6 ms | 8.6 ms | 4× |
| 30,000 | 121.6 ms | 13.9 ms | 9× |
| 60,000 | **538.7 ms** | 29.6 ms | **18×** |

长对话页面（ChatGPT / Gemini / LMArena 攒几十轮后节点数轻松上万到几万）每 200ms 花掉几百毫秒，**等于把该标签页的主线程占满**。已改成 Set 判重（带无 Set 环境的数组回退）。

对象引用判等上 `Array.indexOf`（`===`）和 `Set.has`（SameValueZero）只在 `NaN`/`-0` 上有差异，`seen` 里存的全是 DOM 节点对象，行为完全一致。

### 2. MutationObserver 的自适应节流

原来固定 200ms 去抖，页面越大越扛不住。改成「下次间隔 = 上次扫描耗时 × 4」，钳在 200~800ms。

换成 Set 之后 6 万节点也只要约 30ms，算出来仍是下限 200ms —— **正常页面行为完全不变**；只有单次扫描超过 50ms 的病态页面才会退避，且最多退到 800ms，不会拖慢对 Cloudflare 盾、「出了点问题」这类关键词的识别。

### 3. 标签页唤醒风暴

`_try_wake_tab()` 每次调用会发出 **4~6 次 CDP 往返**：`Emulation.setFocusEmulationEnabled(true)` → 整页可见性补丁 JS → `Page.setWebLifecycleState(active)` → `run_js(readyState)` → `setFocusEmulationEnabled(false)`。

而它挂在 `_get_page_check_snapshot_text()` 的入口上，你 `config/commands.json` 里现在有 6 条启用的 `page_check` 命令（间隔 1~8s，有 observer 时被压到 1.5s），实际调用频率是**每秒数次**。这些 CDP 状态本身是粘性的，重复下发没有意义。

已加按会话限频，默认 2s，环境变量 `CMD_WAKE_TAB_MIN_INTERVAL_SEC` 可调，设 `0` 完全恢复旧行为。同时：JS 执行出错（页面可能真被冻结/丢弃）时立即清除限频，且只要会话上还有未清零的失败计数就不限频——避免「限频 → 连续两轮失败 → 触发 30s 退避」这条回归路径。

### 4. page_check 关键词匹配的重复全文扫描

`_text_contains_needle()` 里每个非 ASCII 关键词都要对**整页快照**做一次 `re.sub(r"\s+", "", hay)`。你有一条命令带 10+ 个中文关键词，快照几十到几百 KB，等于同一段文本被反复扫十几遍。

已加单槽缓存（按字符串**对象身份**比较，零哈希碰撞风险），并把 4 个热路径正则提到模块级预编译。已确认 `_text_contains_needle` 的调用链只在 `cmd-periodic-checker` 单线程里，无并发问题。

### 5. `arena-stream-hard-stop.user.js`：每秒 3 次 React Fiber 全量遍历

`repairActiveStreamState()` 每秒跑一次，但一次 tick 内会通过 `updateOverlay()` → `pendingMessageInfo()`、直接调用、`cleanupStaleStreamController()` → `pendingMessageInfo()` 触发 **3 次** `findStoreFromFiber()`。每次是 4 个 `querySelector` + 最多 80 层 fiber 向上遍历 + 每层 5 个 bucket 的深度 4 递归扫描，**页面完全空闲时也照跑**。

已加 200ms TTL 微缓存，复用前做三重校验：TTL 未过期 + `looksLikeArenaStore` 完整校验 + `state.id` 与缓存时一致且与 URL 里的会话 id 不冲突。切会话/换路由会立刻失效重搜，不会拿到旧 store 让「硬停止」失效。

### 6. `arena-stream-hard-stop.user.js`：`stoppedMessageGuards` 永不回收

清理函数 `cleanupStoppedMessageGuards()` 的唯一调用点在 `healStoppedRerunFailure()` 内部，而该函数第一行就被 `ENABLE_FAILED_RERUN_HEAL = false`（硬编码常量）挡住 —— 清理是死代码。而写入端 `rememberStoppedMessageGuard()` 在每次「硬停止」时正常写入，条目带 30s TTL 却永远不会被删。

已补一个只删过期条目的 `sweepExpiredStoppedGuards()`，在 `repairActiveStreamState()` 开头无条件调用。删除集合是原清理函数删除集合的**严格子集**，不会多删。

### 7. `arena-context-model-lab.user.js`：100ms 轮询定时器叠加

`startModelChangePoll()` 里 `timer` 只是局部变量，没有任何去重（对比紧邻的 `startDomPoll()` 有 `if (domPollTimer) return`）。每次投票点击就新开一个 100ms 定时器跑满 5 秒，**5 秒内连投 N 次就有 N 个并发定时器**，每个 tick 都要跑一次含 fiber 遍历的 `storeSnapshot()`。

已改成与 `startDomPoll` 一致的「已有轮询在跑就跳过」。

---

## 二、值得改但风险偏高，留给你决定

### A.【收益最大】整页文本每 0.5s 完整传回 Python

`_get_page_check_snapshot_text()` 的做法是：

```python
snapshot = self._run_page_check_js(session, "return window.__pcObserver ? String(window.__pcSnapshot || '') : null")
```

把**整页小写化文本**（长对话可达几百 KB～MB）通过 CDP 序列化传回 Python，缓存只有 0.5s，然后在 Python 里做关键词匹配。每个会话每秒一到两次。

但页面内的 `window.__pcHits` **已经算好了每个关键词的命中结果**。理想做法是只回传 `__pcHits`（几十字节），Python 侧只在需要做「整词匹配 / 中文去空白匹配」这类特殊语义时才退回拉全文。

- 收益：CDP 传输量下降 3~4 个数量级，Python 侧正则开销基本归零。
- 风险：Python 侧的匹配语义（`_text_contains_needle` 的整词匹配、中文去空白匹配）比页面内的 `indexOf` 严格，直接切换会改变触发行为。需要把两套语义对齐后再切，属于结构性改动。

顺带：`window.__pcSnapshot` 会把整页文本常驻在每个受控标签页的 JS 堆里。

### B. `latest_visual_reply` 策略下每次轮询逐元素测量几何

`app/core/stream_monitor.py` `_select_candidate_element()`：

```python
for index, ele in enumerate(elements):
    score = ele.run_js("const rect = this.getBoundingClientRect(); ...")
```

流式输出期间每 0.1~1.0s 一轮，每轮对**每个**消息容器发一次 `run_js`。50 个容器就是 50 次 CDP 往返 + 50 次强制同步重排。而且这条路径**跳过了 `prefer_anchor` 快速通道**（第 415 行 `if prefer_anchor and strategy != "latest_visual_reply"`），所以每轮都是全量重扫。

建议：把 N 次 `ele.run_js` 合并成一次 `tab.run_js` 批量测量；或对几何结果加约 1s 的缓存、仅在元素数量变化时重扫。风险在于元素对象跨轮次可能失效，需要实测。

### C. 日志系统：每条 DEBUG 日志约 298 次正则

`app/core/config.py` 的 `_cuteify_debug_message()` 会顺序尝试 72 个函数内联 `re.match` + 21 个预编译模式 + 205 条 `_CUTE_EXTRA_RULES` 兜底表，**未命中就全跑一遍**。且 `SecureLogger._emit()` 只判断日志级别，**不判断是否真有 handler 会输出**。

不过我实测了一下：205 条表驱动扫描约 18.6µs/条，全套约 27µs/条，按 1000 条日志/秒算只占单核约 2.7%。**不是主要瓶颈**，所以没动。

真要省，最快的杠杆是把 `LOG_DEBUG_CUTE_MODE` 设成 `False`（函数第一行就短路，一次正则都不跑），代价是控制台/面板里的 DEBUG 日志变回技术原文。

另外你现在 `.env` 里是 `LOG_LEVEL=DEBUG` + `APP_DEBUG=true`。日常跑改成 `INFO` 能同时省掉正则、字符串拼接、文件 IO 和面板传输——这是**单条改动里性价比最高的**，但会牺牲排障信息，看你取舍。

### D. `_stream_chunk_merge_cache` 峰值内存

`app/core/network_monitor.py`，`MAX_STREAM_CHUNK_MERGE_CACHE = 8`，每份缓存持有一份「当前已合并的完整正文」。超长流式响应（几 MB 正文）时峰值可达正文大小 × 8。**不是泄漏**（每次请求重置），只是峰值。真观测到内存尖峰再把 8 调到 2~3 即可。

### E. `ctx.monitor["media_items"]` 无条数上限

`app/services/request_manager.py:1498`，SSE chunk 回调里只 `extend` 不设上限（对比 `response_parts_chars` 是有上限的）。base64 会被替换成占位符，所以单条只有几十字节，正常请求无影响；只有异常重试/上游重复推流时才会撑到几万条。建议仿照 `response_parts_chars` 加个条数上限。

---

## 三、查过但没问题的（避免你重复排查）

内存这块整体是**干净的**，能看到明显的针对性修复痕迹（注释里的「修复#1」~「修复#7」）。已确认有上限或有清理的：

| 位置 | 机制 | 值 |
|---|---|---|
| `request_manager` 活跃请求 | FIFO + 僵尸清扫线程（60s） | 100 条 / TTL 600s |
| `request_manager` 监控历史 | 二分插入 + 裁剪 | 200 条（可配 0~2000） |
| `request_manager` 响应文本 | 截断 | 30000 字符 |
| `request_manager` SSE 缓冲 | 超限整体清空 | 262144 字符 |
| `network_monitor` 预取响应 | `deque(maxlen)` | 64 |
| `network_monitor` 保留监听 | TTL 过期回收 | 120s |
| `tab_pool` 路由游标 | `OrderedDict` + `popitem` | 1000 |
| `background_image_downloader` | 条数/年龄/总字节三重裁剪，图片流式落盘 | 1000 / 24h / 1GB |
| `image_handler` | 全部落盘，内存无跨请求缓存 | 单张 20MB / 单请求 8 张 |
| Web 日志 deque | `maxlen` | 500 条 |
| 文件日志 | `RotatingFileHandler` | 5MB × 6 ≈ 30MB 封顶 |

其它确认无问题的点：

- **线程池**：三处（`request_manager` 的 save-worker、`tab_pool` 的 `_maintenance_executor`/`_acquire_executor`、图片下载器）全部是单例 + 有 `shutdown()`，**没有每请求新建线程池**。
- **对象生命周期**：`NetworkMonitor` / `StreamMonitor` / `WorkflowExecutor` 都是按请求新建、随请求丢弃，没有全局单例反向持有 tab 导致无法回收。
- **全局网络拦截**：`GLOBAL_NETWORK_INTERCEPTION_ENABLED` 默认 `False`，你也没开。如果哪天开启，注意默认 `listen_pattern` 是 `"http"`（**拦截该标签页的所有请求**），每个空闲标签页一个常驻监听线程，那会是很大一笔开销。
- **前端面板轮询**：日志 1s / 历史 3s / 系统状态 3s，但都做了 `document.visibilityState` 判断，页面隐藏时不轮询。合理。
- **各类后台线程间隔**：浏览器连接看门狗 3~5s、僵尸请求清扫 60s、命令周期调度 1s，都在合理范围。
- **敏感信息脱敏**：13 条正则，但前面有便宜的子串门禁，普通消息一条正则都不跑。是全文里唯一做得比较克制的正则路径。

---

## 四、建议的验证方式

1. 起服务，开 2~3 个长对话标签页，用 Chrome 任务管理器（Shift+Esc）对比改动前后各标签页的 CPU。**改动 1 的效果应该在这里最明显**，尤其是页面正在流式输出时。
2. 空闲状态下观察 Python 进程 CPU，确认改动 3 生效（唤醒 CDP 往返减少）。
3. 功能回归重点看：Cloudflare 盾自动过、「出了点问题」自动刷新、`ds刷新`、Arena 硬停止——这四条都直接依赖被改的代码路径。
4. 如果发现后台标签页被 Chrome 冻结，把 `CMD_WAKE_TAB_MIN_INTERVAL_SEC` 调小或设 `0` 即可完全回到旧行为。
